import { DoubleBetterDirectiveDirective } from './double-better-directive.directive';

describe('DoubleBetterDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DoubleBetterDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
